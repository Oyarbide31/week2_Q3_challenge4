/*
import { calculateLength } from "./main";

describe("Given calculatLength function ", () => {
  describe("When it is call with [2,3,4,5]", () => {
    test("test List lenght is 4 ", () => {
      const list = [2, 3, 4, 5];
      const expected = 4;
      const result = calculateLength(list);
      expect(result).toBe(expected);
    });
  });
};
*/
